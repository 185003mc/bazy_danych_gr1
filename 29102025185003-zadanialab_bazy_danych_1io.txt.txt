zad1
ALTER TABLE zadanie DROP FOREIGN KEY zadanie_pracownik_fk;
ALTER TABLE projekt DROP FOREIGN KEY projket_menadzer_proj_fk;
ALTER TABLE pracownik DROP PRIMARY KEY;
ALTER TABLE pracownik ADD PRIMARY KEY(id_pracownika);

ALTER TABLE zadanie ADD FOREIGN KEY(pracownik) REFERENCES pracownik(id_pracownika) ON DELETE SET NULL;
ALTER TABLE projket ADD FOREIGN KEY(menadzer_proj) REFERENCES pracownik(id_pracownika);

zad2
ALTER TABLE zadanie ADD COLUMN godziny_szacowane INTEGER DEFAULT 8;
UPDATE zadanie SET godziny_szacowane = 4 WHERE id_zadania=1;
UPDATE zadanie SET godziny_szacowane = 12 WHERE id_zadania=2;
UPDATE zadanie SET godziny_szacowane = 16 WHERE id_zadania=3;

ALTER TABLE zadanie MODIFY godziny_szacowane INTEGER DEFAULT 6;

INSERT INTO zadanie(nazwa, priorytet, opis, pracownik, projekt) VALUES
('Testowe', 'normalny', 'Nowe zadanie', 1, 2);
SELECT godziny_szacowane FROM zadanie WHERE nazwa='Testowe';
DELETE FROM zadanie WHERE nazwa ='Testowe';

zad 3

ALTER TABLE projekt ADD COLUMN opis_projektu VARCHAR(50);

ALTER TABLE projekt MODIFY COLUMN opis_projektu VARCHAR(200);

UPDATE projekt SET opis_projektu ='MVP' WHERE id_projektu =1;
UPDATE projekt SET opis_projektu ='Pilne wdrożenie' WHERE id_projektu =2;


ALTER TABLE projekt DROP COLUMN opis_projektu;


Zad4
ALTER TABLE zadanie DROP FOREIGN KEY zadanie_projekt_fk;


ALTER TABLE zadanie ADD FOREIGN KEY(projekt) REFERENCES projekt(id_projektu) ON DELETE SET NULL;

DELETE FROM projekt WHERE id_projektu =1;
SELECT * FROM zadanie WHERE projekt IS NULL;